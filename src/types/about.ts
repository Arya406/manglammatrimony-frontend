export interface AboutPrinciple {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface AboutFeature {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface ApproachStep {
  stepNumber: number;
  stepCode: string;
  title: string;
  description: string;
  icon: string;
}
